namespace Endfield.Tool.GUI.UI;

public sealed record ThemePalette(
    Color FormBack,
    Color Surface,
    Color SurfaceAlt,
    Color Border,
    Color PrimaryText,
    Color SecondaryText,
    Color Accent);
